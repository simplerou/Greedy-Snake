# 复制本文件为 .env（.env 已被 git 忽略，不会被提交），去掉注释后填写真实值。
#
# 填入与线上部署相同的云数据库连接串，本地启动 uvicorn 时就会直接读写云端库，
# 与线上玩家共享同一份全球榜单；不创建 .env 时默认连接本机 MySQL。
#
# 阿里云 RDS MySQL 示例：
# DATABASE_URL=mysql+pymysql://<账号>:<密码>@rm-xxxxxxxx.mysql.rds.aliyuncs.com:3306/greedy_snake?charset=utf8mb4
#
# 注意：密码里的特殊字符必须 URL 编码，否则会被当成连接串的分隔符。
# 例如密码以 @ 结尾时要写成 %40，密码里的 # 写成 %23、/ 写成 %2F、: 写成 %3A。
DATABASE_URL=mysql+pymysql://用户名:密码@主机:端口/greedy_snake?charset=utf8mb4
